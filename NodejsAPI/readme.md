## Install
```bash
npm install
```

- add `db_config.json` file to `./database`
- then add database access authorization

  ```json
  {
    "host" : "IP",
    "user" : "USERNAME",
    "password" : "PASSWORD",
    "database" : "DATABASE"
  }
  ```

## Start
```bash
npm start
```
